# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Sequence,
    Callable,
    ClassVar,
    NoReturn,
    TypeVar,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


LiteralString = str
# -- template models.py.jinja --
import os
import logging
import inspect
import warnings
from collections import OrderedDict

from pydantic import BaseModel, Field

from . import types, enums, errors, fields, bases
from ._types import FuncType
from ._compat import model_rebuild, field_validator
from ._builder import serialize_base64
from .generator import partial_models_ctx, PartialModelField


log: logging.Logger = logging.getLogger(__name__)
_created_partial_types: Set[str] = set()

class Survey(bases.BaseSurvey):
    """Represents a Survey record"""

    SurveyID: _int
    PersonID: _int
    Timestamp: datetime.datetime
    Title: _str
    questions: Optional[List['models.Question']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.SurveyKeys']] = None,
        exclude: Optional[Iterable['types.SurveyKeys']] = None,
        required: Optional[Iterable['types.SurveyKeys']] = None,
        optional: Optional[Iterable['types.SurveyKeys']] = None,
        relations: Optional[Mapping['types.SurveyRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.SurveyKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Survey_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Survey_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Survey_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Survey_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Survey_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Survey_relational_fields:
                        raise errors.UnknownRelationalFieldError('Survey', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Survey / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Survey',
            }
        )
        _created_partial_types.add(name)


class Question(bases.BaseQuestion):
    """Represents a Question record"""

    QuestionID: _int
    SurveyID: _int
    Type: 'enums.enumQtype'
    Text: _str
    Order: _int
    survey: Optional['models.Survey'] = None
    optionalAnswers: Optional[List['models.OptionalAnswer']] = None
    userAnswers: Optional[List['models.UserAnswer']] = None
    followingQuestions: Optional[List['models.OptionalAnswer']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.QuestionKeys']] = None,
        exclude: Optional[Iterable['types.QuestionKeys']] = None,
        required: Optional[Iterable['types.QuestionKeys']] = None,
        optional: Optional[Iterable['types.QuestionKeys']] = None,
        relations: Optional[Mapping['types.QuestionRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.QuestionKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Question_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Question_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Question_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Question_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Question_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Question_relational_fields:
                        raise errors.UnknownRelationalFieldError('Question', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Question / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Question',
            }
        )
        _created_partial_types.add(name)


class OptionalAnswer(bases.BaseOptionalAnswer):
    """Represents a OptionalAnswer record"""

    AnswerID: _int
    QuestionID: _int
    Text: _str
    Order: _int
    followingQuestionID: Optional[_int] = None
    question: Optional['models.Question'] = None
    followingQuestion: Optional['models.Question'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.OptionalAnswerKeys']] = None,
        exclude: Optional[Iterable['types.OptionalAnswerKeys']] = None,
        required: Optional[Iterable['types.OptionalAnswerKeys']] = None,
        optional: Optional[Iterable['types.OptionalAnswerKeys']] = None,
        relations: Optional[Mapping['types.OptionalAnswerRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.OptionalAnswerKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _OptionalAnswer_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _OptionalAnswer_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _OptionalAnswer_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _OptionalAnswer_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _OptionalAnswer_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _OptionalAnswer_relational_fields:
                        raise errors.UnknownRelationalFieldError('OptionalAnswer', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid OptionalAnswer / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'OptionalAnswer',
            }
        )
        _created_partial_types.add(name)


class UserAnswer(bases.BaseUserAnswer):
    """Represents a UserAnswer record"""

    PersonID: _int
    QuestionID: _int
    AnswerID: _int
    Text: Optional[_str] = None
    Location: Optional[_str] = None
    Timestamp: datetime.datetime
    question: Optional['models.Question'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.UserAnswerKeys']] = None,
        exclude: Optional[Iterable['types.UserAnswerKeys']] = None,
        required: Optional[Iterable['types.UserAnswerKeys']] = None,
        optional: Optional[Iterable['types.UserAnswerKeys']] = None,
        relations: Optional[Mapping['types.UserAnswerRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.UserAnswerKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _UserAnswer_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _UserAnswer_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _UserAnswer_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _UserAnswer_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _UserAnswer_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _UserAnswer_relational_fields:
                        raise errors.UnknownRelationalFieldError('UserAnswer', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid UserAnswer / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'UserAnswer',
            }
        )
        _created_partial_types.add(name)


class UserAnswerNew(bases.BaseUserAnswerNew):
    """Represents a UserAnswerNew record"""

    id: _int
    idNumber: _str
    fullName: _str
    settlementCode: _int
    accessibility: _bool
    pets: _bool
    numberOfPeople: _int
    hotelOption1: Optional[_str] = None
    hotelOption2: Optional[_str] = None
    hotelOption3: Optional[_str] = None
    selectedHotel: Optional[_str] = None
    settlement: Optional['models.Settlement'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.UserAnswerNewKeys']] = None,
        exclude: Optional[Iterable['types.UserAnswerNewKeys']] = None,
        required: Optional[Iterable['types.UserAnswerNewKeys']] = None,
        optional: Optional[Iterable['types.UserAnswerNewKeys']] = None,
        relations: Optional[Mapping['types.UserAnswerNewRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.UserAnswerNewKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _UserAnswerNew_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _UserAnswerNew_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _UserAnswerNew_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _UserAnswerNew_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _UserAnswerNew_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _UserAnswerNew_relational_fields:
                        raise errors.UnknownRelationalFieldError('UserAnswerNew', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid UserAnswerNew / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'UserAnswerNew',
            }
        )
        _created_partial_types.add(name)


class Settlement(bases.BaseSettlement):
    """Represents a Settlement record"""

    id: _int
    code: _int
    name: _str
    alias1: Optional[_str] = None
    alias2: Optional[_str] = None
    alias3: Optional[_str] = None
    userAnswers: Optional[List['models.UserAnswerNew']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.SettlementKeys']] = None,
        exclude: Optional[Iterable['types.SettlementKeys']] = None,
        required: Optional[Iterable['types.SettlementKeys']] = None,
        optional: Optional[Iterable['types.SettlementKeys']] = None,
        relations: Optional[Mapping['types.SettlementRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.SettlementKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Settlement_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Settlement_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Settlement_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Settlement_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Settlement_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Settlement_relational_fields:
                        raise errors.UnknownRelationalFieldError('Settlement', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Settlement / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Settlement',
            }
        )
        _created_partial_types.add(name)



_Survey_relational_fields: Set[str] = {
        'questions',
    }
_Survey_fields: Dict['types.SurveyKeys', PartialModelField] = OrderedDict(
    [
        ('SurveyID', {
            'name': 'SurveyID',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('PersonID', {
            'name': 'PersonID',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('Timestamp', {
            'name': 'Timestamp',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('Title', {
            'name': 'Title',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('questions', {
            'name': 'questions',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Question\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Question_relational_fields: Set[str] = {
        'survey',
        'optionalAnswers',
        'userAnswers',
        'followingQuestions',
    }
_Question_fields: Dict['types.QuestionKeys', PartialModelField] = OrderedDict(
    [
        ('QuestionID', {
            'name': 'QuestionID',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('SurveyID', {
            'name': 'SurveyID',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('Type', {
            'name': 'Type',
            'is_list': False,
            'optional': False,
            'type': 'enums.enumQtype',
            'is_relational': False,
            'documentation': None,
        }),
        ('Text', {
            'name': 'Text',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('Order', {
            'name': 'Order',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('survey', {
            'name': 'survey',
            'is_list': False,
            'optional': True,
            'type': 'models.Survey',
            'is_relational': True,
            'documentation': None,
        }),
        ('optionalAnswers', {
            'name': 'optionalAnswers',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.OptionalAnswer\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('userAnswers', {
            'name': 'userAnswers',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.UserAnswer\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('followingQuestions', {
            'name': 'followingQuestions',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.OptionalAnswer\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_OptionalAnswer_relational_fields: Set[str] = {
        'question',
        'followingQuestion',
    }
_OptionalAnswer_fields: Dict['types.OptionalAnswerKeys', PartialModelField] = OrderedDict(
    [
        ('AnswerID', {
            'name': 'AnswerID',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('QuestionID', {
            'name': 'QuestionID',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('Text', {
            'name': 'Text',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('Order', {
            'name': 'Order',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('followingQuestionID', {
            'name': 'followingQuestionID',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('question', {
            'name': 'question',
            'is_list': False,
            'optional': True,
            'type': 'models.Question',
            'is_relational': True,
            'documentation': None,
        }),
        ('followingQuestion', {
            'name': 'followingQuestion',
            'is_list': False,
            'optional': True,
            'type': 'models.Question',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_UserAnswer_relational_fields: Set[str] = {
        'question',
    }
_UserAnswer_fields: Dict['types.UserAnswerKeys', PartialModelField] = OrderedDict(
    [
        ('PersonID', {
            'name': 'PersonID',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('QuestionID', {
            'name': 'QuestionID',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('AnswerID', {
            'name': 'AnswerID',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('Text', {
            'name': 'Text',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('Location', {
            'name': 'Location',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('Timestamp', {
            'name': 'Timestamp',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('question', {
            'name': 'question',
            'is_list': False,
            'optional': True,
            'type': 'models.Question',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_UserAnswerNew_relational_fields: Set[str] = {
        'settlement',
    }
_UserAnswerNew_fields: Dict['types.UserAnswerNewKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('idNumber', {
            'name': 'idNumber',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('fullName', {
            'name': 'fullName',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('settlementCode', {
            'name': 'settlementCode',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('accessibility', {
            'name': 'accessibility',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('pets', {
            'name': 'pets',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('numberOfPeople', {
            'name': 'numberOfPeople',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('hotelOption1', {
            'name': 'hotelOption1',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('hotelOption2', {
            'name': 'hotelOption2',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('hotelOption3', {
            'name': 'hotelOption3',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('selectedHotel', {
            'name': 'selectedHotel',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('settlement', {
            'name': 'settlement',
            'is_list': False,
            'optional': True,
            'type': 'models.Settlement',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Settlement_relational_fields: Set[str] = {
        'userAnswers',
    }
_Settlement_fields: Dict['types.SettlementKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('code', {
            'name': 'code',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('alias1', {
            'name': 'alias1',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('alias2', {
            'name': 'alias2',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('alias3', {
            'name': 'alias3',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('userAnswers', {
            'name': 'userAnswers',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.UserAnswerNew\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)



# we have to import ourselves as relation types are namespaced to models
# e.g. models.Post
from . import models, actions

# required to support relationships between models
model_rebuild(Survey)
model_rebuild(Question)
model_rebuild(OptionalAnswer)
model_rebuild(UserAnswer)
model_rebuild(UserAnswerNew)
model_rebuild(Settlement)
